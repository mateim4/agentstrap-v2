# Handoff — where we left off

_Cross-device session handoff for **{{PROJECT_NAME}}**. Keeps only the last ~2 weeks of session narrative — older sessions move to the permanent [[Work Log]] (vault root, append-only, newest first). Durable operational facts (machine topology, how to run the stack, gotchas) belong in their own note, not here; credentials belong only in [[Credentials and secrets]]. The narrative is maintained by Claude (via `/agentstrap:handoff` or when wrapping up); the auto-state block at the bottom is written by hooks every turn. On a new machine the `SessionStart` hook injects this file automatically._

## Session YYYY-MM-DD — (title)

- (pending first narrative update)

## What's next (priority order)

1. (pending)

## Blockers / open items

- (none noted)

## Key docs to read first

- (none noted)

## Open questions in flight

- (see Open Questions in the vault)

<!-- AGENTSTRAP:AUTO-STATE -->
## Auto state (written automatically every turn — do not hand-edit)
- Updated: (pending first hook run)
<!-- /AGENTSTRAP:AUTO-STATE -->

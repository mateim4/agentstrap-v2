# Delta Tracking — {{PROJECT_NAME}}

_Compact, timestamped change log written automatically by the `Stop` and `SessionEnd` hooks (one entry per turn / session). Newest entries are appended at the bottom. Read alongside `{{HANDOFF_FILE}}` to reconstruct where work stands._

<!-- Entries are appended below this line. Format:
### [YYYY-MM-DD HH:MM] <machine> — <branch>
- Focus: <last user request>
- Working tree: <N> uncommitted file(s)
-->
